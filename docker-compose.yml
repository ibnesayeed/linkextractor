version: '3'

services:
  api:
    image: linkextractor-api:step4-python
    build: ./api
    expose:
      - "5000"
  web:
    image: php:7-apache
    ports:
      - "80:80"
    environment:
      - API_ENDPOINT=http://api:5000/api/
    volumes:
      - ./www:/var/www/html
